<%= render "shared/errors", object: @comment %>
<h1 class="mb-4"> Editing comment from <%=@post.title %> </h1>

<%= render "form", post: @post, comment: @comment %>
<%= link_to "Back to post", post_path(@post), class: "inline-block bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded" %>