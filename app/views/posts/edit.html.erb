<%= render "shared/errors", object: @post %>
<h1> Editing <%=@post.title %> </h1>

<%= render "form", post: @post %>
<%= link_to "Back to post", post_path(@post), class: "inline-block bg-gray-600 hover:bg-gray-500 text-white px-4 py-2 rounded" %>